<div dir="rtl">

# הגדרות — איפה כל דבר יושב

הכלים כאן אינם מנחשים נתיבים, מודלים או מספרי שרשור. **מה שאינו
מוגדר — הכלי עוצר ואומר, ולא ממציא.**

## התקנה

```bash
cp config.example.json ~/.otogit.json    # ואז לערוך
export OTOGIT_REPO="owner/repo"          # חובה לכלי ה-shell
```

## מה צריך למלא, ואיפה למצוא

| שדה | איפה למצוא אצלך |
|---|---|
| `repo` | ‏`owner/name` של הריפו |
| `discussions.board` | מספר השרשור לדיווחי סוכנים |
| `discussions.decisions` | מספר השרשור לשאלות שדורשות אותך |
| `agents.*.bin` | ‏`Get-Command grok \| Select Source` |
| `agents.*.key_env` | **שם משתנה הסביבה**, לא המפתח עצמו |
| `local_models.models_dir` | ‏`$env:OLLAMA_MODELS`, או ברירת המחדל של ollama |
| `local_models.roles` | ‏`ollama list` — איזה מודל לאיזו משימה |

## ⚠️ מה שלא נכנס לקובץ הזה, לעולם

**מפתחות, טוקנים וסיסמאות.** הקובץ מחזיק את **שם** משתנה הסביבה
(`"key_env": "GEMINI_KEY_1"`), לא את ערכו. מי שקורא אותו שולף את
הערך מהסביבה בזמן ריצה.

הסיבה מדודה: מפתח שנכנס לגיט נשאר בהיסטוריה גם אחרי מחיקה, ובוטים
סורקים ריפואים ציבוריים תוך **דקות** מהעלאה.

## תפקידים למודלים מקומיים — לפי מדידה, לא לפי גודל

נמדד על CPU בלי GPU (‏64GB RAM, ‏16 ליבות):

| מודל | מהירות | למה מתאים |
|---|---|---|
| `llama3.2:3b` | **‏13 tok/s** | טריאז', סיווג, תיוג — פלט קצר |
| `qwen2.5-coder:7b` | ~‏9-11 tok/s | קריאת קוד, זיהוי כפילויות |
| `phi4:14b` | **‏3.1 tok/s** | לוגיקה — כבר איטי לאינטראקטיבי |
| `mixtral:8x7b` | ~‏1-2 tok/s | ‏MoE, החכם ביותר שעוד שווה — רקע בלבד |

**המחסום אינו RAM אלא רוחב-פס לזיכרון:** המודל נקרא מהזיכרון בכל
טוקן. ‏70B בלי GPU ≈ מילה בשנייה — אין לו שימוש מעשי.

**מודלים מקומיים אינם מחליפים את הענן** — הם המנוע שממשיך לעבוד
כשכל השאר מחזירים `429`.

</div>
