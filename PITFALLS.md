<div dir="rtl">

# מלכודות שנלמדו בדם

**כל אחת כאן עלתה בזמן אמת.** הן נרשמות כדי שהסוכן הבא לא יגלה אותן
מחדש — ובעיקר כדי שמי שכתב את הכלל יקיים אותו על עצמו.

---

## ‏1. ‏`\n` בתוך heredoc של Python נהיה שורה חדשה אמיתית

**קרה שלוש פעמים ביום אחד, ובכל פעם עלה זמן.** ‏`python - <<'PY'`
נראה מצוטט, אבל שכבות המעטפת שביניהן יכולות לפרש `\n` לפני שהוא מגיע
ל-Python. התוצאה: מחרוזת שנשברת לשתי שורות, ‏YAML פגום, או `printf`
שמדפיס שורה חדשה במקום `\n`.

**הכשל נראה כך:**

```
    printf(stderr, "fanout: target %s
", buf);          ← אמור להיות \n
```

```yaml
              if not rules.endswith("
"):                                     ← YAML שבור
```

**מה שעובד:**

- לערוך את הקובץ בכלי עריכה ישיר, לא בהחלפת מחרוזת דרך מעטפת
- לבנות את התו בקוד: `chr(10)` או `'\' + 'n'`
- לבדוק **אחרי** כל כתיבה: ‏`bash -n`, ‏`yaml.safe_load`, או
  ‏`compile()` — ולא להניח שההחלפה הצליחה

---

## ‏2. ‏`set -e` אינו חל על pipeline שמתחיל ב-`!`

שומר שנראה כך **אינו מסוגל להיכשל**:

```sh
set -eu
! grep -R -nE '(dd|wipefs|mkfs)' .
echo "guard: PASS"
```

נמדד על `dash` מול קובץ שמכיל `dd if=/dev/zero of=/dev/sda`: הדפיס
`guard: PASS`, יצא **0**. ‏shellcheck מסמן `SC2251` — אבל רק אם הוא
מותקן, וה-CI שם דילג עליו בשקט.

---

## ‏3. ‏PowerShell 5.1 מוחק מרכאות כפולות בדרך לתוכנית חיצונית

```powershell
gh issue list --jq '... index("agent:claimed") ...'
```

מגיע ל-jq כ-`index(agent:claimed)` ונשבר על הנקודתיים. **הופיע
בשלושה מקומות באותו סקריפט.** הפתרון אינו ציטוט חכם יותר — אלא **לא
לשלוח מחרוזות ל-jq בכלל**: לפרסר ב-PowerShell או בפייתון.

---

## ‏4. ‏`git checkout -- <path>` אחרי `git add` משחזר מה-index

בקרה שלילית שמריצה `git checkout -- src/` אחרי שכבר עשית `git add`
משחזרת את **הקוד המתוקן על עצמו**. הטסטים עוברים, ומדפיסים `PASS`
במקום שבו הכישלון היה הראיה.

**הצורה הנכונה:** ‏`git checkout <base-sha> -- <path>`, ואז **`grep -c`
על הסמל שהוסר** — ראיה חיובית שהוא באמת איננו.

---

## ‏5. עבודה גמורה שנשארת ב-worktree בלי push

קרה **שלוש פעמים**: ה-PR הראה קוד ישן בזמן שהתיקון היה staged מקומית.
`git status` וה-remote סיפרו שני סיפורים שונים.

**אחרי כל push:** ‏`git show origin/<branch>:<file>` — לא הקובץ המקומי.

---

## ‏6. קוד יציאה 0 שאינו אומר כלום

- ‏`codex exec` נעצר בשער אמון, יצא **0**, והלוג היה 115 בתים
- ‏`git clone --shared` הצליח ונתן עץ **8 קומיטים אחורה**
- ‏`gh` יוצא 0 גם על רשימה ריקה
- ‏`--jq` על נתיב שאינו קיים מחזיר 0 עם פלט ריק

**המדד אינו קוד היציאה — הוא שיצא משהו.** לבדוק גודל פלט, לקרוא ערך
בחזרה, להשוות sha.

---

## ‏7. ‏`id()` של אובייקט שנאסף לזבל ממוחזר

טסט שמשווה `id()` של אובייקטים כדי להוכיח שהם שונים ייכשל אקראית:
תהליכון שסיים, האובייקט שלו נאסף, ואובייקט חדש קיבל את אותה כתובת.
**לשמור את האובייקטים עצמם**, לא את ה-id.

---

## הכלל שמאחורי כולן

**‏`|| true`, ‏`2>/dev/null`, ‏`SilentlyContinue`, ‏`skipif` — לפני כל
אחד מהם, לשאול מה קורה כשהבדיקה עצמה נכשלת.** אם התשובה היא "נראה
כמו הצלחה", זה באג.

</div>
