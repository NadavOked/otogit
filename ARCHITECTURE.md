<div dir="rtl">

# למה זה בנוי על git, ולא על שרת

זו ההכרעה שממנה נגזר כל השאר. היא נאמרה על ידי בעל הריפו:

> זה נועד לקוד ול-git. אם תשתמש בו בערך — יהיה לך git.

## התזה

**‏git הוא התשתית היחידה שמובטח שתהיה שם.** כל פרויקט שמישהו ירצה
להריץ עליו את זה — יש לו git, וברוב המקרים גם GitHub. ולכן אין
צורך בשום דבר נוסף:

| מה שצריך | איפה זה יושב | מה **לא** נדרש |
|---|---|---|
| תור משימות | תוויות על Issues | ‏Redis · SQS · מסד נתונים |
| נעילה | ‏`git push` ל-ref ייעודי — ‏CAS אמיתי בצד השרת | שירות נעילה |
| לוח דיווח | Discussions | ‏Slack · דאשבורד |
| מתזמן | ‏GitHub Actions (`schedule`) | ‏cron על שרת · systemd timer |
| היסטוריה וביקורת | הקומיטים עצמם | מסד לוגים |
| הרשאות | ‏branch protection ו-scope של ה-token | ‏IAM |

**אפס תשתית לתחזק, ואפס דבר שיכול ליפול בשקט.** בפרויקט שבו הכלל
הוא ש"לא הצלחנו לבדוק" אינו "בדקנו והכל תקין", שרת נוסף הוא
התחייבות רעה: הוא עוד משהו שיכול למות בלי שאיש ישים לב.

## ההשלכה: הכלי לא יודע דבר על הפרויקט

מכאן נגזר הדפוס שכבר עובד: **הכלי כללי, וריפו היעד מספק את
המשמעות.** לא הפוך.

| הקובץ בריפו היעד | מה הוא אומר לכלי |
|---|---|
| `.otogit/agent-policy.txt` | אילו נתיבים אסורים לסוכן כאן |
| `.otogit/review-rules.txt` | לפי אילו כללים לסקור קוד כאן |
| `tools/agents/providers.json` | אילו מודלים יש, מה המגבלות, ומה סדר הניתוב |

הכלי **מסרב לנחש**. חסר `agent-policy.txt` — הרץ עוצר ומשחרר את
התביעה, ולא ממציא מדיניות. זה לא נימוס: רץ שנושא איתו את הנתיבים
האסורים של פרויקט אחר יאסור את הדברים הלא נכונים **ויתיר את
הנכונים**, וזה גרוע מהיעדר הגנה כי הוא נראה כמו הגנה.

## מה זה אומר למי שמוסיף כאן משהו

1. **אל תניח מבנה של פרויקט.** אין `server/`, אין `agent/`, אין
   שפה. אם הכלי צריך לדעת משהו — שריפו היעד יצהיר עליו בקובץ.
2. **אל תוסיף תלות בשירות חיצוני.** אם משהו דורש חשבון, מפתח או
   שרת — הוא כנראה שייך לריפו של הפרויקט, לא לכאן.
3. **אל תקבע ערך שאפשר לגזור.** מספר שנכתב ביד מתיישן בשקט: בעל הריפו
   החליף חומרה, וזמני המודלים בקובץ המשיכו להיראות כמו עובדה.
   ‏`bench-local.py` מודד; ‏shellcheck גוזר את סוג המעטפת
   מה-shebang. החלטה שאפשר לגזור אין סיבה לנחש.
4. **כל כלי נכשל בקול.** יציאה לא-אפסית עם נימוק. ‏`|| true`,
   ‏`2>/dev/null` ו-`SilentlyContinue` הופכים "לא הצלחנו" ל"הצליח",
   וזה הבאג היחיד שהפרויקט הזה קיים כדי למנוע.

## מה שנשאר פתוח

**הנעילה עדיין מייעצת.** ‏`agent-claim.sh` מודה בזה בהערה שלו:
קריאה־בחזרה מצמצמת את החלון ואינה סוגרת אותו, כי ל-REST של גיטהאב
אין CAS על תוויות. הכיוון שנמדד — ‏`git push` ל-`refs/claims/*`,
שהוא CAS אמיתי — מתועד ב-הפרויקט שממנו זה נולד ו**טרם מומש כאן**.

זה גם המבחן של התזה: אם התשובה לנעילה נמצאת בתוך git עצמו, התזה
עומדת. אם היא תדרוש שירות חיצוני — היא לא.

</div>
